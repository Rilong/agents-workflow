# Codex Agent Model Mapping

- [x] Confirm the requested model mapping for Codex agents.
- [x] Update Codex agent configs for `devil`, `developer`, and `tester`.
- [x] Review the final config state and capture results.

## Review

- `devil` now uses `model = "gpt-5.5"` with `model_reasoning_effort = "high"`.
- `developer` now uses `model = "gpt-5.4"` with `model_reasoning_effort = "medium"`.
- `tester` now uses `model = "gpt-5.4"` with `model_reasoning_effort = "medium"`.
